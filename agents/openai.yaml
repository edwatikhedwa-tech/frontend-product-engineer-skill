interface:
  display_name: "Frontend Product Engineer"
  short_description: "Build and audit frontend products with browser evidence."
  brand_color: "#0F172A"
  default_prompt: "Use $front. Determine CREATE, REDESIGN, EXTEND, or REVIEW mode, then follow the browser-first audit protocol and quality gates."

policy:
  allow_implicit_invocation: true

# Interaction patterns

Study the transition and feedback around search, filters, selection, bulk actions, popovers, dialogs, tabs, pagination, inline editing, and split views.

Record trigger, discoverability, keyboard behavior, focus return, loading, success, error, cancellation, undo, and responsive behavior. Reuse the interaction logic only after checking the current product’s semantics and constraints.

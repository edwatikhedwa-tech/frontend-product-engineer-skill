# Screen patterns

Useful screen families include searchable lists, bulk-action tables, filter builders, split views, company profiles, detail panels, settings navigation, and operational dashboards.

For each screen record entry point, primary task, information order, density strategy, responsive strategy, empty/loading/error states, and the smallest reusable pattern. Avoid treating a screenshot’s colors or radius as the transferable lesson.
